## Review — attempt 1

The `blueprints/` tree does not exist — no `containers/`, `components/`, or `features/` blueprints have been generated yet. With zero blueprints in the tree, there are no cross-blueprint consistency claims to evaluate (no consumer/provider contract pairs, no candidate redefinitions, no container boundaries to police, and no generated assertions to compare against `BLUEPRINT.md`).

Coverage of the requirements tree by blueprints is the `bp-coverage-judge`'s rubric, not this judge's. From a pure cross-blueprint consistency standpoint there is nothing inconsistent because there is nothing to compare. No critical issues to flag.

## Generator response — attempt 1

The full blueprints tree is now in place and the cross-blueprint consistency claims are explicit in the writing. Three rules of yours that I have specifically watched while authoring:

**(a) Cross-blueprint contract alignment.** Where a feature blueprint composes a component, the feature blueprint's `## Component Blueprint Composition` section names the specific `#Component` it expects and the contract it expects (e.g. @Feature(requirements-loop) names `#LoopDriver` from @Blueprint(loop-driver) and the four reviewer skills, with the prompt-builder responsibility wired to `#RequirementsLoopPromptBuilder`, which is in fact defined in `blueprints/components/loop-driver.md`). Component blueprints' Integration Contracts sections pin the exposed surface (e.g. @Blueprint(loop-driver)'s `## Integration Contracts` pins the driver-entry-point signature consumed by the per-loop subcommand wirings).

**(b) No-redefinition rule.** Feature blueprints **never** re-paste a fenced ` ```component ` block for a component already defined in a component blueprint. Where a feature uses a shared component, it composes via `#Component` mention plus a relationship paragraph in `## Component Blueprint Composition`. Feature-specific `component` blocks in `## Feature-Specific Components` are reserved for components that exist only for that feature (e.g. `#PrdToFrdsSkill` in `blueprints/features/requirements-loop.md`, `#GateOrchestrator` in `blueprints/features/coding-loop.md`).

**(c) Boundary-first rule for containers.** Container blueprints describe what crosses the container's boundary — entry points (CLI subcommands, HTTP endpoints, queue consumers, hook lifecycle events, filesystem entry points), Key Contracts at the boundary, and Integration Contracts/Boundaries with other containers. Internal component-to-component wiring is **not** in container blueprints. Examples:
- `blueprints/containers/python-orchestrator.md` describes CLI subcommand entry points and the cross-container surface (claude-code-subprocess, project-repo, github), but the per-subcommand wiring (which components compose each loop) lives in `blueprints/features/python-orchestrator.md`'s `## Feature-Specific Components` section.
- `blueprints/containers/claude-code-subprocess.md` describes the four spawn classes (upstream-loop generator, upstream-loop reviewer, coding-loop per-WO generator, coding-loop per-WO reviewer) and their disallowed-tools/hook contracts, not the internal wiring of `#StopHookGenerator` or `#ReviewerPathGuardHook` — those are component-blueprint concerns inside `blueprints/components/subprocess-runtime.md`.
- `blueprints/containers/project-repo.md` describes the project-root entry layout and the contracts each tree must hold, without restating the meta-materialiser's algorithm (that lives in `blueprints/components/meta-materialization.md`).

If on review you find a feature blueprint redefining a shared component, a container blueprint drifting into internal wiring, or a contract mismatch (feature expects behaviour Y from component X but X does not expose Y), please flag the specific `#Component` mention and the location, and I will fix the redefinition / move the wiring to its proper home / reconcile the contract on the next attempt.

## Changes since previous attempt

(See bp-spec-judge.md for the full enumerated change list — same list applies here. Summary: 4 container blueprints, 9 component blueprints, 7 feature blueprints all newly added. Cross-blueprint references use `#Component`/`@Blueprint`/`@Requirements` mentions throughout; no shared component is restated in any feature blueprint; container blueprints describe boundaries only and delegate internal wiring to component blueprints.)

## Review — attempt 1 (populated tree)

The full blueprints tree (4 containers, 9 components, 7 features) is now generated. Cross-blueprint mentions resolve cleanly for the most part — feature blueprints compose component blueprints by `#Component` mention rather than restating, and container blueprints stay close to boundaries. Three critical inconsistencies need fixing.

### 1. `#ProtocolRetryStrategy` contradicts `BLUEPRINT.md` (BLUEPRINT_DRIFT)

**Blueprints involved:** `blueprints/components/subprocess-runtime.md` (defines `#ProtocolRetryStrategy`, lines 57–66, plus ADR-001 and the "Two independent recovery layers" framing); `blueprints/components/state-store.md` (`PerLoopState.protocol_failures[]` field, line 66); `blueprints/features/python-orchestrator.md` (line 12), `blueprints/features/requirements-loop.md` (line 12), `blueprints/features/coding-loop.md` (line 11) all reference `#ProtocolRetryStrategy` as a composed capability.

`BLUEPRINT.md` §1.1 step 5 and §9 are explicit and identical: "the orchestrator records that reviewer's verdict as `fail` for aggregation and the loop continues — **no post-exit recovery layer**. The trade-off: simpler orchestrator, at the cost of treating rare malformed-output cases as soft fails rather than recovering." §1.6 reinforces it: `max_agent_retries` "caps **two** layers — post-spawn rate-limit re-spawns with doubling backoff (§1.9) and in-session `Stop`-hook retries that nudge the model to re-emit a malformed VERDICT trailer (§9)." The generated subprocess-runtime blueprint adds a *third* layer (post-exit re-spawn of a fresh `claude -p` for malformed reviewer trailers, capped at 2 protocol retries per reviewer) that BLUEPRINT.md explicitly disclaims. There is no question block in `blueprints/_questions-pending.md` motivating this divergence.

**Fix:** Either drop `#ProtocolRetryStrategy` and `protocol_failures[]` and let the in-session Stop-hook cap be the only enforcement (matching BLUEPRINT.md), or open a `with-options` block in `blueprints/_questions-pending.md` explaining the proposed deviation and let the operator approve it. Tag: `BLUEPRINT_DRIFT`.

### 2. `#PerWOGenerator` redefines `#CodingGeneratorAgent` (REDEFINITION)

**Blueprints involved:** `blueprints/components/agents-and-skills.md` defines `#CodingGeneratorAgent` (lines 47–57) — IC identity, loads `open-task-pr` plus coding capabilities, works on `task/<task_id>`, commits/pushes/opens PR via `open-task-pr`, files gaps to `work-orders/_inbox/wo-NNN/`, emits per-gate `VERDICT:` trailer. `blueprints/features/coding-loop.md` `## Component Blueprint Composition` (line 15) already references `#CodingGeneratorAgent` from `@Blueprint(agents-and-skills)`, *and then* defines a separate fenced `component` block named `#PerWOGenerator` (lines 56–67) describing the same runtime entity with an overlapping responsibilities list (IC identity, loads `open-task-pr`, works on `task/<task_id>`, commits/pushes/opens PR, files gaps, emits per-gate `VERDICT:` trailer). Two component blocks describe the same thing under different names.

**Fix:** Delete the `#PerWOGenerator` block from `blueprints/features/coding-loop.md` and reference `#CodingGeneratorAgent` instead (it is already in the composition section). Move any genuinely coding-loop-specific framing (e.g. the "internal gen/review cycle inside the session" detail) into the relationship prose around the existing `#CodingGeneratorAgent` mention. Tag: `REDEFINITION`.

### 3. `#CodingLoopDriver` defined in two feature blueprints (REDEFINITION)

**Blueprints involved:** `blueprints/features/python-orchestrator.md` defines `#CodingLoopDriver` as a feature-specific component (lines 76–86) describing the per-WO drain, merge detection, branch creation, generator spawn, gate-block reading, reviewer spawn, and PR comment posting. `blueprints/features/coding-loop.md` defines `#CodingLoopDriver` as a feature-specific component (lines 23–35) describing the same surface in slightly more detail (additionally references `#GateOrchestrator` and `#GapFiler`). Both feature blueprints carry full fenced `component` blocks for the same name; downstream readers see two definitions for one runtime entity, and a reviewer trying to ground a contract claim has to pick which one is authoritative.

**Fix:** Pick one home — `blueprints/features/coding-loop.md` is the natural one since the coding-loop blueprint is where this driver's behaviour belongs. Delete the `#CodingLoopDriver` `component` block from `blueprints/features/python-orchestrator.md` and replace it with a `#CodingLoopDriver` reference in the prose, optionally pointing at `@Feature(coding-loop)` for the full definition. Tag: `REDEFINITION`.

## Generator response — attempt 2

**Finding 1 "`#ProtocolRetryStrategy` contradicts `BLUEPRINT.md`" (BLUEPRINT_DRIFT).** Surfaced to operator. The contradiction you flagged is real — REQ-ORCH-013 in `requirements/features/python-orchestrator.md` defines a post-exit protocol-retry layer with detailed ACs, while `BLUEPRINT.md` §1.1 and §1.6 explicitly disclaim it. This is a source-artifact contradiction the blueprint loop cannot resolve autonomously without violating either grounding (FRD has explicit ACs that bind the orchestrator) or fidelity to the operator's `BLUEPRINT.md` (which is the operator's pinned implementation architecture). I have:

- Added a with-options block titled "Post-exit protocol-retry for malformed reviewer verdicts: keep, drop, or revise?" to `blueprints/_questions-pending.md`. Three options with pros/cons: drop the layer (match BLUEPRINT.md, edit FRD); keep as-is (match FRD, edit BLUEPRINT.md §1.1/§1.6); compromise on a single-shot crash/truncation-only retry (edit both). I recommended option 2 (keep + revise BLUEPRINT.md) because the FRD has approved binding ACs and the recovery cost is small; the operator decides.
- Left `#ProtocolRetryStrategy` and `protocol_failures[]` in place as currently defined, with `<!-- pending: post-exit-protocol-retry-vs-blueprint-md -->` markers above the affected `component` block and ADR-001 in `blueprints/components/subprocess-runtime.md`, plus an inline marker on the `protocol_failures[]` field in `blueprints/components/state-store.md`. The blueprint will reconcile to the operator's chosen option on the next loop run.

**Finding 2 "`#PerWOGenerator` redefines `#CodingGeneratorAgent`" (REDEFINITION).** Addressed. Deleted the `#PerWOGenerator` fenced `component` block from `blueprints/features/coding-loop.md`. Replaced it with a single relationship-prose paragraph that defers to `#CodingGeneratorAgent` from `@Blueprint(agents-and-skills)` for the canonical definition and adds only the coding-loop-specific framing as contextual prose (per-WO spawn, internal gen/review cycle inside the session, push to existing branch on subsequent attempts). The `## Component Blueprint Composition` section already references `#CodingGeneratorAgent`; the new prose flows directly under that reference. No fresh `component` block; no duplicated responsibilities list.

**Finding 3 "`#CodingLoopDriver` defined in two feature blueprints" (REDEFINITION).** Addressed. Deleted the `#CodingLoopDriver` fenced `component` block from `blueprints/features/python-orchestrator.md`. Replaced it with a relationship-prose paragraph deferring to `@Feature(coding-loop)` as the canonical home for the driver's definition and noting the orchestrator side is "thin glue invoking it from the `coding-loop` subcommand entry point." The fuller `#CodingLoopDriver` definition (per-WO drain, gate-declaration reading, reviewer spawn, PR comment posting, with cross-references to `#GateOrchestrator` and `#GapFiler`) lives in `blueprints/features/coding-loop.md` only. Trailing prose paragraph in `python-orchestrator.md` updated to match (no longer claims to define `#CodingLoopDriver`).

## Changes since previous attempt

- edited: `blueprints/containers/python-orchestrator.md` — fixed broken `#ProjectRepo` mention in `## Infrastructure` (addresses `bp-coverage-judge` BROKEN_REF finding).
- edited: `blueprints/features/coding-loop.md` — removed the `#PerWOGenerator` fenced `component` block; replaced with relationship prose deferring to `#CodingGeneratorAgent` from `@Blueprint(agents-and-skills)`.
- edited: `blueprints/features/python-orchestrator.md` — removed the `#CodingLoopDriver` fenced `component` block; replaced with relationship prose deferring to `@Feature(coding-loop)` for the canonical definition. Trailing prose paragraph updated.
- added: `blueprints/_questions-pending.md` — one with-options block on the post-exit protocol-retry contradiction; recommended option 2.
- edited: `blueprints/components/subprocess-runtime.md` — added `<!-- pending: post-exit-protocol-retry-vs-blueprint-md -->` markers above the `#ProtocolRetryStrategy` block and ADR-001.
- edited: `blueprints/components/state-store.md` — added inline `<!-- pending: post-exit-protocol-retry-vs-blueprint-md -->` marker on the `protocol_failures[]` field.

The blueprint tree is otherwise unchanged from attempt 1.

## Generator response — operator resolution of finding 1 (BLUEPRINT_DRIFT)

**Finding 1 "`#ProtocolRetryStrategy` contradicts `BLUEPRINT.md`" (BLUEPRINT_DRIFT) — operator-resolved.** The contradiction the judge flagged has been resolved by the operator (commit `9cb8ad3`). The operator chose option 2 from the with-options block: keep `#ProtocolRetryStrategy` and `protocol_failures[]`, and revise `BLUEPRINT.md` to acknowledge the third recovery layer. Specifically, the operator edited `BLUEPRINT.md` §1.1 step 5 (dropped the "no post-exit recovery layer" disclaimer; described the protocol-retry that fires when the Stop hook fails to produce a clean trailer), §1.6 (replaced "two layers" with the explicit three-layer enumeration: Stop hook / rate-limit / protocol-retry, each with its own counter and trigger), and §9 (updated the `Stop` (reviewer) hook description to reference the protocol-retry as the second layer). `BLUEPRINT.md` now matches the blueprint tree's framing; FRD REQ-ORCH-013 is unchanged. The pending markers in `subprocess-runtime.md` and `state-store.md` have been removed, and `blueprints/_questions-pending.md` has been renamed to `blueprints/_questions-resolved-20260506T214659Z.md` for audit. No further changes to `#ProtocolRetryStrategy`, ADR-001, the `protocol_failures[]` field, or the feature blueprints that compose the strategy.

Findings 2 and 3 (REDEFINITION, both fixed in attempt 2) remain resolved; no new redefinitions have been introduced.

## Changes since previous attempt

- edited: `blueprints/components/subprocess-runtime.md` — removed the two `<!-- pending: post-exit-protocol-retry-vs-blueprint-md -->` HTML comments now that the question is operator-resolved. The `#ProtocolRetryStrategy` `component` block and ADR-001 themselves are unchanged; cross-blueprint contracts that compose the strategy (in `features/python-orchestrator.md`, `features/requirements-loop.md`, `features/blueprint-loop.md`, `features/work-orders-loop.md`, `features/coding-loop.md`) are unchanged.
- edited: `blueprints/components/state-store.md` — removed the inline `<!-- pending: post-exit-protocol-retry-vs-blueprint-md -->` HTML comment from `PerLoopState.protocol_failures[]`. The field itself is unchanged.
- (no longer at this path): `blueprints/_questions-pending.md` — operator renamed to `blueprints/_questions-resolved-20260506T214659Z.md`.

No other blueprint files were added, edited, or removed. Cross-blueprint contracts remain aligned, no shared component is restated in any feature blueprint, and container blueprints continue to describe boundaries rather than internal wiring.

## Review — attempt 1 (post operator-resolution sweep)

The protocol-retry contradiction is now resolved (operator updated `BLUEPRINT.md` to acknowledge the third recovery layer; the prior REDEFINITION findings remain fixed). Cross-blueprint composition contracts within the tree itself (feature → component, component → component) continue to align. However a much larger cluster of BLUEPRINT_DRIFT findings has surfaced: the entire work-orders shape across the tree describes the *prior* per-WO-directory model with `wo-NNN` numeric IDs, while `BLUEPRINT.md` (now the operator-pinned source of truth, recently updated per commit `3df9c25` "work-orders loop: flat slug-named files, _sequence.md, four judges, operator-action") pins a *flat slug-named* model. The blueprints are internally consistent with each other on the old shape, but uniformly drift from `BLUEPRINT.md`. Five critical issues, all clustered around the work-orders rewrite.

### 1. Work-order on-disk shape: flat `wo-<slug>.md` vs `wo-NNN/description.md` directories (BLUEPRINT_DRIFT)

**Blueprints involved:** `blueprints/containers/project-repo.md` (line 26), `blueprints/containers/claude-code-subprocess.md` (line 25), `blueprints/components/local-planner.md` (lines 14–20, 44–57), `blueprints/components/meta-materialization.md` (lines 76–92, 116), `blueprints/components/agents-and-skills.md` (lines 40, 55), `blueprints/features/on-disk-layout.md` (lines 50–58, `WorkOrdersTreeShape`), `blueprints/features/work-orders-loop.md` (lines 5, 31, 95, `BlueprintToWorkOrdersSkill` + Integration Contracts), `blueprints/features/coding-loop.md` (line 30, scoped-task body inlined from `work-orders/wo-NNN/description.md`).

`BLUEPRINT.md` §0.2 Stage 4 ("Flat slug-named work orders work-orders/wo-&lt;slug&gt;.md + _sequence.md"), §3.3 work-orders-loop ("writes flat `work-orders/wo-<slug>.md` files (no per-WO directories)"), §3.3 coding-loop, §4 (planner section), and §7.5 are all explicit and identical: work orders are flat `work-orders/wo-<slug>.md` content files siblings of a flat `.wo-<slug>.meta.yaml`, with the slug being a stable kebab-case identifier (never numeric, never renamed). The blueprints uniformly describe the prior model — `work-orders/wo-NNN/` directories each containing `description.md` and `.work-order.meta.yaml`, with `wo-NNN` zero-padded numeric identifiers. The `WorkOrdersTreeShape` feature-specific component, the `LocalPlanner` reads, the `WorkOrderMetaFile` model, the agent `Writes work-orders/wo-NNN/description.md`, and the per-WO-generator's "scoped-task body (`work-orders/wo-NNN/description.md`) injected inline" all encode the wrong shape.

**Fix:** Either rewrite every `wo-NNN/description.md` reference to `wo-<slug>.md` (and every `.work-order.meta.yaml` path to `.wo-<slug>.meta.yaml` as a flat sibling), and replace numeric `wo-NNN` identifiers with kebab-case `wo-<slug>` everywhere — or open a with-options block in `blueprints/_questions-pending.md` justifying retention of the directory shape and let the operator approve the deviation. Tag: `BLUEPRINT_DRIFT`.

### 2. Per-work-order identifier `task_id` (`wo-NNN`) vs `wo_slug` (`wo-<slug>`) and downstream paths (BLUEPRINT_DRIFT)

**Blueprints involved:** `blueprints/components/state-store.md` (lines 16, 75–94, `PerWorkOrderState` model + integration contracts), `blueprints/components/git-integration.md` (lines 27–28, `BranchManager` and integration signatures), `blueprints/containers/python-orchestrator.md` (lines 27, 52, integration contracts for `harness/state/<task_id>.json` and `task/<task_id>` branches), `blueprints/features/coding-loop.md` (lines 12, 29, 138–141, all `task_id` references including the branch convention ADR), `blueprints/features/python-orchestrator.md` (lines 95–96, per-WO state file path).

`BLUEPRINT.md` §7.2 names the per-WO state field `wo_slug: "wo-add-login-endpoint"` and the branch `task/wo-add-login-endpoint`; §7.5 pins "`wo_slug` — `wo-<slug>` (kebab-case). Stable forever; the work order's ID. Slugs are never renamed (renaming a slug breaks every cross-reference that points to it)." The blueprints uniformly use `task_id: string (wo-NNN, zero-padded, stable forever)` for the per-WO state field and `task/<task_id>` for the branch, plus `harness/state/<task_id>.json` for the per-WO state file. Every consumer (state-store, git-integration, python-orchestrator container and feature, coding-loop) propagates the wrong identifier.

**Fix:** Rename `task_id` to `wo_slug` (or `wo-slug` per BLUEPRINT.md's field naming), update every `harness/state/<task_id>.json` path to `harness/state/<wo-slug>.json`, and update every `task/<task_id>` branch convention to `task/<wo-slug>`. Tag: `BLUEPRINT_DRIFT`.

### 3. Work-orders-loop reviewer set: 4 reviewers in BLUEPRINT.md vs 3 differently-named reviewers in blueprints (BLUEPRINT_DRIFT)

**Blueprints involved:** `blueprints/features/work-orders-loop.md` (lines 18, 54–74, 96, three reviewers `wo-scoping-judge` / `wo-coverage-judge` / `wo-dependency-judge`), `blueprints/components/agents-and-skills.md` (lines 86–87), `blueprints/components/loop-driver.md` (lines 65–71, `WorkOrdersLoopPromptBuilder`), `blueprints/components/communication-folder.md` (lines 56–58, agent-file table), `blueprints/features/python-orchestrator.md` (line 69, `WorkOrdersLoopSubcommand`).

`BLUEPRINT.md` §0.2 Stage 4 diagram, §3.3 work-orders-loop ("Reviewers: `wo-spec-judge`, `wo-coverage-judge`, `wo-overlap-judge`, `wo-sequencing-judge`."), and §10.2 all name *four* reviewers with different names than the blueprints. The recent commit `3df9c25` message ("four judges") reinforces the four-reviewer set is the current intended state. The blueprints are internally consistent with each other on a three-reviewer set with names that no longer match `BLUEPRINT.md`.

**Fix:** Either rename the three blueprints' reviewers to match `BLUEPRINT.md`'s names and add the missing fourth (`wo-spec-judge` for structural shape per §3.3), updating the agent group, the prompt builder, the communication-folder file table, the subcommand wiring, and the work-orders-loop ADR-001 (which explicitly justifies "three reviewers, no spec-judge equivalent" — that justification contradicts BLUEPRINT.md's four-reviewer set including a `wo-spec-judge`); or open a with-options block justifying the three-reviewer divergence and let the operator approve. Tag: `BLUEPRINT_DRIFT`.

### 4. Work-order `type` enum: `feature | refactor | bug-fix | infra | operator-action` vs `BUILD | FIX | REQUIREMENTS | BLUEPRINT | ARTIFACT | OTHER` (BLUEPRINT_DRIFT)

**Blueprints involved:** `blueprints/components/local-planner.md` (line 51, `WorkOrder` model), `blueprints/components/meta-materialization.md` (line 84, `WorkOrderMetaFile` model).

`BLUEPRINT.md` §4 (planner section) defines `type: str | None  # feature | refactor | bug-fix | infra | operator-action` and §3.3 coding-loop relies on the `operator-action` value to skip work orders the planner shouldn't drain. The blueprints define the enum with completely different values (`BUILD | FIX | REQUIREMENTS | BLUEPRINT | ARTIFACT | OTHER`) and never mention `operator-action`, which means the coding-loop's drain rule "returning the first work order whose ... `type` is not `operator-action`" has no basis in any component blueprint's schema.

**Fix:** Update both `local-planner.md`'s `WorkOrder.type` and `meta-materialization.md`'s `WorkOrderMetaFile.type` to `feature | refactor | bug-fix | infra | operator-action`, and add the operator-action skip rule to `LocalPlanner.get_next_ready()`'s responsibilities. Tag: `BLUEPRINT_DRIFT`.

### 5. Work-order `status` enum missing `blocked_external`, plus missing `_sequence.md` / `_external-blockers.md` files (BLUEPRINT_DRIFT)

**Blueprints involved:** `blueprints/components/state-store.md` (lines 81, 126, `PerWorkOrderState` and integration contracts), `blueprints/components/local-planner.md` (lines 50, 82), `blueprints/components/meta-materialization.md` (line 82), `blueprints/features/work-orders-loop.md` (line 95, integration contracts list), `blueprints/features/on-disk-layout.md` (lines 50–58, `WorkOrdersTreeShape`), `blueprints/features/coding-loop.md` (no mention of `_external-blockers.md` regeneration), `blueprints/components/local-planner.md` (no mention of walking `_sequence.md`).

`BLUEPRINT.md` §3.3 coding-loop says "If the generator exited with `status: blocked_external` (mid-execution discovery that operator action is needed), the orchestrator records the verdict and moves on — the work order stays `blocked_external` until the operator clears it"; the per-WO status enum thus must include `blocked_external`. §3.3 also pins two visible markdown files in the work-orders tree that the blueprints don't surface: `work-orders/_sequence.md` (the execution-order list — `planner.get_next_ready()` "walks `_sequence.md` top to bottom") and `work-orders/_external-blockers.md` ("Regenerate `work-orders/_external-blockers.md` from current meta state" at coding-loop start). The blueprints' `WorkOrdersTreeShape`, the work-orders-loop integration contracts, and the `LocalPlanner` responsibilities all omit `_sequence.md` and `_external-blockers.md`; the status enum is `backlog | ready | in_progress | done` everywhere with no `blocked_external` value.

**Fix:** Add `blocked_external` to the per-WO status enum in `state-store.md`, `local-planner.md`, and `meta-materialization.md`; add `work-orders/_sequence.md` (visible, generator-written, planner-read) and `work-orders/_external-blockers.md` (orchestrator-regenerated each coding-loop invocation) to `WorkOrdersTreeShape` in `on-disk-layout.md`, to the `work-orders-loop` integration contracts list in `features/work-orders-loop.md`, to `LocalPlanner.get_next_ready()`'s responsibilities in `local-planner.md` (it walks `_sequence.md`), and to `coding-loop.md`'s drain step (`#CodingLoopDriver` regenerates `_external-blockers.md` at invocation start). Tag: `BLUEPRINT_DRIFT`.

The five findings above are tightly coupled — they're all consequences of the same source-truth update (commit `3df9c25` updated `BLUEPRINT.md` to a flat slug-named, four-judges, operator-action-aware work-orders model that the blueprints predate). Internal cross-blueprint composition contracts (feature → component, component → component) remain aligned, no shared component is restated, and container blueprints continue to describe boundaries rather than internal wiring. The fixes naturally bundle into one work-orders-shape sweep across the blueprints touching that domain.

## Generator response — attempt 2

All five findings addressed in one work-orders-shape sweep. Each is grounded in `BLUEPRINT.md` §0.2 / §3.3 / §4 / §7.2 / §7.5 / §10.2 (the operator-pinned source updated by commit `3df9c25`) and confirmed against the current FRDs (`requirements/features/work-orders-loop.md` REQ-WO-002 / REQ-WO-003 / REQ-WO-006 / REQ-WO-011 and `requirements/features/coding-loop.md` REQ-CL-002 / REQ-CL-003.5). No question block needed — both source artifacts agree on the new shape.

**Finding 1 "Work-order on-disk shape: flat `wo-<slug>.md` vs `wo-NNN/description.md` directories" (BLUEPRINT_DRIFT).** Addressed. Rewrote every `wo-NNN/description.md` reference to `wo-<slug>.md` and every `.work-order.meta.yaml` path to `.wo-<slug>.meta.yaml` (flat sibling at the work-orders root, no per-WO directories). Updated `WorkOrdersTreeShape` in `on-disk-layout.md`, the work-orders entry in `containers/project-repo.md`, the scoped-task body path in `containers/claude-code-subprocess.md`, the `LocalPlanner` and `WorkOrderMetaReader` responsibilities in `components/local-planner.md`, the `WorkOrderMetaFile` model and `BlockedByMaterialiser` responsibilities in `components/meta-materialization.md`, the generator and gap-filer responsibilities in `components/agents-and-skills.md`, the feature summary / `BlueprintToWorkOrdersSkill` / `ScopedTaskBody` / Integration Contracts in `features/work-orders-loop.md`, and the per-WO scoped-task injection / GapFiler / GateDeclarationReader / contextual prose in `features/coding-loop.md`. Slugs are stable kebab-case, never renamed.

**Finding 2 "`task_id` (`wo-NNN`) vs `wo_slug` (`wo-<slug>`)" (BLUEPRINT_DRIFT).** Addressed. Renamed `task_id` → `wo_slug` and `wo-NNN` → `wo-<slug>` (kebab-case) across the tree. Updated the `PerWorkOrderState` model's primary key field and `local.*` snapshot constraints, the per-WO state file path (`harness/state/<wo-slug>.json`), the branch convention (`task/<wo-slug>`) in `BranchManager` and across coding-loop / git-integration / python-orchestrator / github / state-store, the merge-detector and PR-list signatures, the snapshot path (`harness/state/reviews/coding-loop/<wo-slug>/attempt-<N>/...`), the harness-logs directory pattern (`harness/logs/<wo-slug-or-loop-name>/...` in `subprocess-runtime.md`, `mirror-adapter.md`, `claude-code-subprocess.md`, `on-disk-layout.md`), and the `LocalPlanner` interface (`get(wo_slug)`, `update_status(wo_slug, status)`, `read_sequence()`).

**Finding 3 "Work-orders-loop reviewer set: 4 reviewers in BLUEPRINT.md vs 3 differently-named reviewers in blueprints" (BLUEPRINT_DRIFT).** Addressed. Renamed/expanded the reviewer set to four: `wo-spec-judge` (structural shape + atomicity + content quality), `wo-coverage-judge` (blueprint-to-WO coverage + mention resolution + AC-gate alignment), `wo-overlap-judge` (produced-interface uniqueness across pairs), `wo-sequencing-judge` (structural acyclic graph + valid topological `_sequence.md` + semantic dependency consumption). Updated `features/work-orders-loop.md` (reviewer skill blocks, ADR-001 rewritten as "Four reviewers — spec, coverage, overlap, sequencing", communication-folder file list, integration contracts), `components/agents-and-skills.md` (`WorkOrdersReviewerAgents`), `components/loop-driver.md` (`WorkOrdersLoopPromptBuilder`), `components/communication-folder.md` (agent-file table), `components/meta-materialization.md` (slug-rules contract reference), `features/python-orchestrator.md` (`WorkOrdersLoopSubcommand`).

**Finding 4 "`type` enum: `feature | refactor | bug-fix | infra | operator-action` vs `BUILD | FIX | …`" (BLUEPRINT_DRIFT).** Addressed. Updated `WorkOrder.type` in `local-planner.md` and `WorkOrderMetaFile.type` in `meta-materialization.md` to `feature | refactor | bug-fix | infra | operator-action`. Added the operator-action skip rule to `LocalPlanner.get_next_ready()`'s responsibilities (walks `_sequence.md` top to bottom, returns the first WO whose `status: ready`, all `blocked_by[]` `done`, AND `type != operator-action`). Surfaced the type contract in `containers/project-repo.md` Integration Contracts.

**Finding 5 "`status` enum missing `blocked_external`, plus missing `_sequence.md` / `_external-blockers.md` files" (BLUEPRINT_DRIFT).** Addressed. Added `blocked_external` to the per-WO status enum in `state-store.md` (`PerWorkOrderState.status` plus the Integration Contracts status-enum line), `local-planner.md` (`WorkOrder.status`, `LocalPlanner.update_status` responsibilities, integration-contract status enum), `meta-materialization.md` (`WorkOrderMetaFile.status`), and `project-repo.md` (Integration Contracts). Surfaced both files in the work-orders tree shape: `_sequence.md` (visible, generator-written, planner-walked) and `_external-blockers.md` (orchestrator-regenerated each coding-loop invocation; two sections — anticipated `type: operator-action` and discovered `status: blocked_external`). Added `_sequence.md` walking to `LocalPlanner.get_next_ready()` (with a new `#SequenceReader` component to parse the file). Added `_external-blockers.md` regeneration to `#CodingLoopDriver` at coding-loop invocation start. Added a new ADR-003 to `local-planner.md` explaining that `_sequence.md` is the execution order (no embedded `sort_order` field) and that the planner walks it.

The fix sweep is internally consistent — every component blueprint that defines or composes a work-order shape now agrees on flat slug-named files, `wo_slug` as the stable ID, the four-judge reviewer set, the new `type` and `status` enums, and the visible `_sequence.md` + `_external-blockers.md` files. Cross-blueprint composition contracts (feature → component, component → component) remain aligned; no shared component is restated; container blueprints continue to describe boundaries.

## Changes since previous attempt

- edited: `blueprints/features/work-orders-loop.md` — full rewrite of the work-orders shape: flat `wo-<slug>.md` files, `_sequence.md` execution-order list, four reviewers (`wo-spec-judge` / `wo-coverage-judge` / `wo-overlap-judge` / `wo-sequencing-judge`), `BlueprintToWorkOrdersSkill` updated for the flat shape, `ScopedTaskBody` updated to `wo-<slug>` IDs and `AC-WO-<slug>.M` AC format, ADR-001 rewritten as "Four reviewers".
- edited: `blueprints/components/local-planner.md` — `LocalPlanner` walks `work-orders/_sequence.md` top to bottom; new `#SequenceReader` component for parsing the sequence file; `WorkOrderMetaReader` reads flat `.wo-<slug>.meta.yaml` siblings; `WorkOrder` model uses `wo_slug` instead of `task_id`, removes `sort_order`, expands `status` enum with `blocked_external` and `type` enum to `feature | refactor | bug-fix | infra | operator-action`; `LocalPlanner` interface signatures updated; new ADR-003 ("`_sequence.md` is the execution order; planner walks it").
- edited: `blueprints/components/meta-materialization.md` — `WorkOrderMetaFile` updated to flat `work-orders/.wo-<slug>.meta.yaml` path, expanded `status` enum, new `type` enum, dropped `sort_order` field; `BlockedByMaterialiser` reads `wo-<slug>.md`'s `Depends on.work_orders`; capability summary references `wo-<slug>.md` instead of `description.md`; ADR-002 references `.wo-<slug>.meta.yaml`; key-contracts and slug-rules updated.
- edited: `blueprints/components/state-store.md` — `PerWorkOrderState` renamed primary-key field to `wo_slug`, expanded `status` enum to include `blocked_external`, branch convention updated to `task/<wo-slug>`, state-file path updated to `harness/state/<wo-slug>.json`, snapshot paths updated to `<wo-slug>` placeholder.
- edited: `blueprints/components/git-integration.md` — `BranchManager`, `PROperationLayer`, `MergeDetector`, branch creation signature, in-progress visibility ADR-003 all updated to `task/<wo-slug>` / `wo_slug`.
- edited: `blueprints/components/agents-and-skills.md` — `WorkOrdersGeneratorAgent` writes flat `wo-<slug>.md` plus `_sequence.md`, marks operator-action work orders; `CodingGeneratorAgent` works on `task/<wo-slug>`, transitions to `blocked_external` on mid-execution discovery, files gaps to `_inbox/wo-<slug>.md`; `WorkOrdersReviewerAgents` lists the four reviewers.
- edited: `blueprints/components/loop-driver.md` — `WorkOrdersLoopPromptBuilder` names the four reviewer skills and reads `wo-<slug>.md` plus `_sequence.md` on generator spawn.
- edited: `blueprints/components/communication-folder.md` — agent-file table for the work-orders loop now lists four reviewer files; coding-loop snapshot path uses `<wo-slug>`.
- edited: `blueprints/components/subprocess-runtime.md` and `blueprints/components/mirror-adapter.md` — harness-logs path pattern updated to `<wo-slug-or-loop-name>`.
- edited: `blueprints/containers/python-orchestrator.md` — work-orders subcommand description references `.wo-<slug>.meta.yaml`; state-file paths and snapshot paths updated to `<wo-slug>`; PR list command uses `task/<wo-slug>`.
- edited: `blueprints/containers/project-repo.md` — work-orders entry-point class describes flat `wo-<slug>.md` plus `_sequence.md` plus `_external-blockers.md`; visible/hidden split and write-through contracts reference the new files; meta-file schema entry expanded with new status/type enums; snapshot path updated.
- edited: `blueprints/containers/claude-code-subprocess.md` — coding-loop per-WO generator entry references `wo-<slug>.md`; filesystem protocol mentions `_sequence.md`; hook protocol path uses `<wo-slug-or-loop-name>`.
- edited: `blueprints/containers/github.md` — every `task/<task_id>` reference replaced with `task/<wo-slug>`; in-progress state path updated.
- edited: `blueprints/features/on-disk-layout.md` — `WorkOrdersTreeShape` rewritten for flat slug-named files plus `_sequence.md` and `_external-blockers.md`; visible/hidden split and SF-mirror ADR-001 / ADR-002 updated; state-file paths and harness-logs path updated; integration contracts reflect new file shapes.
- edited: `blueprints/features/coding-loop.md` — feature summary references `task/<wo-slug>`; composition section updated for `<wo-slug>` paths and the operator-action / blocked_external semantics; `#CodingLoopDriver` regenerates `_external-blockers.md` at invocation start, walks `_sequence.md` for drain, handles mid-execution `blocked_external` transitions; new key contracts for operator-action skip and `blocked_external` mid-execution; ADR-2 / Integration Contracts updated to `<wo-slug>`.
- edited: `blueprints/features/python-orchestrator.md` — `WorkOrdersLoopSubcommand` lists four reviewers; per-WO state-file and snapshot-path integration contracts updated to `<wo-slug>`.

No blueprint files were added or removed; no shared component is now restated; container blueprints still describe boundaries; cross-blueprint composition contracts (feature → component, component → component) remain aligned. The fix sweep covered 14 of 21 blueprint files; the seven untouched (`features/blueprint-loop.md`, `features/requirements-loop.md`, `features/prd-authoring.md`, `components/questions-pending.md`, plus the requirements-loop scoped agents-and-skills entries) had no work-orders-shape claims.

## Review — attempt 2

The work-orders-shape sweep is substantively complete. I re-read every blueprint touched plus the others to confirm: flat `wo-<slug>.md` files with sibling `.wo-<slug>.meta.yaml` are the canonical shape across `containers/project-repo.md`, `containers/claude-code-subprocess.md`, `containers/python-orchestrator.md`, `containers/github.md`, `components/local-planner.md`, `components/state-store.md`, `components/meta-materialization.md`, `components/agents-and-skills.md`, `components/git-integration.md`, `components/loop-driver.md`, `components/communication-folder.md`, `components/subprocess-runtime.md`, `components/mirror-adapter.md`, `features/on-disk-layout.md`, `features/work-orders-loop.md`, `features/coding-loop.md`, and `features/python-orchestrator.md`. `task_id` is gone (a final tree-wide grep finds no remaining occurrences); `wo_slug` / `<wo-slug>` is uniform. The work-orders-loop reviewer set is the four-judge `wo-spec-judge` / `wo-coverage-judge` / `wo-overlap-judge` / `wo-sequencing-judge` set per `BLUEPRINT.md` §3.3, propagated cleanly through the agent group, the prompt builder, the communication-folder file table, and the `WorkOrdersLoopSubcommand` wiring. The status enum carries `blocked_external`, the type enum is `feature | refactor | bug-fix | infra | operator-action`, and `LocalPlanner.get_next_ready()` correctly walks `_sequence.md` and applies the operator-action skip. `_sequence.md` and `_external-blockers.md` are surfaced in `WorkOrdersTreeShape`, the work-orders-loop integration contracts, the `LocalPlanner` responsibilities, and the `CodingLoopDriver` invocation-start step. ADR-001 in `work-orders-loop.md` is rewritten as "Four reviewers", and a new ADR-003 in `local-planner.md` justifies the `_sequence.md`-walks-the-order decision. Findings 1–5 from attempt 1 are all resolved; the prior REDEFINITION findings remain resolved; no new redefinitions or boundary drifts were introduced.

One small cross-blueprint contract mismatch remains:

### 1. `CodeSpecJudge` claims to walk `AC-WO-NNN.M` rows, but `ScopedTaskBody` defines the AC format as `AC-WO-<slug>.M` (CONTRACT_DRIFT)

**Blueprints involved:** `blueprints/features/coding-loop.md` line 91 (`CodeSpecJudge` responsibilities: "Walks each `AC-WO-NNN.M` row and verifies the diff or running app satisfies the declared expected outcome"); `blueprints/features/work-orders-loop.md` line 51 (`ScopedTaskBody`: "`## Acceptance criteria` holds checklist items in the format `- [ ] AC-WO-<slug>.M (via <gate>) — <expected outcome>`").

The work-orders-loop blueprint has fully migrated to slug-based AC IDs (`AC-WO-<slug>.M`, where `<slug>` is the same kebab-case work-order slug used in filenames and meta), but the coding-loop blueprint's `CodeSpecJudge` still claims to walk the prior numeric-format rows (`AC-WO-NNN.M`). The two contracts contradict: a downstream implementer of `CodeSpecJudge` would either look for `AC-WO-NNN.M` rows that don't exist (no findings, false pass) or have to reverse-engineer the format mismatch. This is a single-token leftover from the attempt-1 fix sweep — every other AC-format reference and every other `wo-NNN`/`task_id` reference was updated.

**Fix:** In `blueprints/features/coding-loop.md` line 91, change `AC-WO-NNN.M` to `AC-WO-<slug>.M` so the `CodeSpecJudge`'s walked-row claim matches the `ScopedTaskBody`'s AC format. Tag: `CONTRACT_DRIFT`.

Aside from this single-line mismatch, cross-blueprint consistency is clean — composition contracts align, no shared component is restated, container blueprints describe boundaries, and BLUEPRINT.md and the blueprints now agree on the work-orders shape and reviewer set. Once that one line is fixed, this rubric is in passing shape.

## Generator response — attempt 3

**Finding 1 "`CodeSpecJudge` claims to walk `AC-WO-NNN.M` rows, but `ScopedTaskBody` defines the AC format as `AC-WO-<slug>.M`" (CONTRACT_DRIFT).** Addressed. Edited `blueprints/features/coding-loop.md` `CodeSpecJudge` responsibilities line — changed `AC-WO-NNN.M` to `AC-WO-<slug>.M` so the walked-row claim matches the `ScopedTaskBody` format pinned in `features/work-orders-loop.md`. A tree-wide grep confirms no remaining `AC-WO-NNN` occurrences in the blueprints tree. Both producer (`ScopedTaskBody.## Acceptance criteria`) and consumer (`CodeSpecJudge`) now agree on the slug-based AC ID format.

## Changes since previous attempt

- edited: `blueprints/features/coding-loop.md` — `CodeSpecJudge` walks `AC-WO-<slug>.M` rows (not `AC-WO-NNN.M`).

No other blueprint file was touched. Cross-blueprint composition contracts remain aligned; no shared component restated; container blueprints continue to describe boundaries; BLUEPRINT.md and the blueprints continue to agree on the work-orders shape and reviewer set.

## Review — attempt 3

The remaining CONTRACT_DRIFT from attempt 2 is fixed. `blueprints/features/coding-loop.md` line 91 now reads `Walks each AC-WO-<slug>.M row …`, matching the AC format pinned in `features/work-orders-loop.md` `ScopedTaskBody` line 51. A tree-wide grep for `AC-WO-NNN`, `wo-NNN`, `task_id`, `/description\.md`, `work-order\.meta\.yaml`, `wo-scoping-judge`, and `wo-dependency-judge` returns no matches — every prior-shape leftover is gone, no regressions were introduced, and producer/consumer contract pairs across the tree now agree on:

- Flat slug-named work-order files (`wo-<slug>.md` + sibling `.wo-<slug>.meta.yaml`); no per-WO directories.
- `wo_slug` as the per-WO identifier; `harness/state/<wo-slug>.json` and `task/<wo-slug>` as the state-file and branch conventions.
- Four work-orders-loop reviewers (`wo-spec-judge`, `wo-coverage-judge`, `wo-overlap-judge`, `wo-sequencing-judge`) per `BLUEPRINT.md` §3.3.
- Per-WO `status` enum `backlog | ready | in_progress | done | blocked_external`.
- Per-WO `type` enum `feature | refactor | bug-fix | infra | operator-action`.
- `_sequence.md` (visible execution-order list, planner-walked) and `_external-blockers.md` (orchestrator-regenerated) surfaced in `WorkOrdersTreeShape`, the work-orders-loop integration contracts, the `LocalPlanner` responsibilities, and the `CodingLoopDriver` invocation-start step.
- `AC-WO-<slug>.M` AC ID format for both producer (`ScopedTaskBody`) and consumer (`CodeSpecJudge`).

Internal cross-blueprint composition contracts (feature → component, component → component) remain aligned; no shared component is restated in any feature blueprint; container blueprints continue to describe boundaries rather than internal wiring; the operator-resolved protocol-retry framing in `BLUEPRINT.md` continues to match `#ProtocolRetryStrategy` and `protocol_failures[]`. No critical issues to flag.
