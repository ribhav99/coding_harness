---
name: prd-authoring
description: Interactive PRD (Product Overview) authoring. Helps the operator draft and refine PRD sections at `requirements/overview/{section-slug}/document.md` in the project repo. Optional — operators can also write the PRD by hand. Once the PRD exists, run `prd-to-frds` to decompose it into per-feature FRDs.
---

# PRD Authoring

TODO: skill body. Downstream: `prd-to-frds`.
