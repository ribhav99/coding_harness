---
name: prd-authoring
description: Interactive PRD (Product Overview) authoring. Helps the operator draft and refine a PRD at `projects/{slug}/overview/{section-slug}/requirements/document.md`. Optional — operators can also write the PRD by hand. Once the PRD exists, run `prd-to-frds` to decompose it into per-feature FRDs.
---

# PRD Authoring

TODO: skill body. Downstream: `prd-to-frds`.
