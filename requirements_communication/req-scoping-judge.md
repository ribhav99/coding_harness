## Review

Walked the seven leaf FRDs in `requirements/features/` (prd-authoring, requirements-loop, blueprint-loop, work-orders-loop, coding-loop, on-disk-layout, python-orchestrator). Tree is flat — no `<slug>_children/` directories — so parent/child correctness is trivially satisfied. Each FRD passes the four feature-unit criteria, and the new work-orders-loop / slimmed coding-loop pair shows clean separation with no overlap.

Each loop FRD delivers standalone value (a triggerable subcommand producing a distinct artifact tree or PR), carries its own implementation footprint (subcommand + generator skill + reviewer set + state file), can be deployed and tested independently of the others (each consumes the prior loop's committed output), and adds incremental value beyond its dependencies. `prd-authoring` is appropriately scoped as the operator-facing manual stage. `on-disk-layout` and `python-orchestrator` are cross-cutting infrastructure FRDs that have been part of the canonical feature set throughout — treating them as feature units is consistent with prior reviews and keeps the layout/orchestrator contracts in their own document.

The new work-orders-loop FRD (REQ-WO-001..010) and the slimmed coding-loop FRD (REQ-CL-001..007) cleanly split the prior fused coding-loop into "decompose blueprints into work orders" (work-orders-loop) and "drain the ready queue" (coding-loop). The work-orders-loop FRD explicitly says it produces no per-WO implementation; the coding-loop FRD explicitly says it does not generate or refresh the work-order sequence. AC-CL-001.3 and AC-WO-001.4 each reference the other to anchor the boundary. No requirement appears in both; no requirement falls between them.

## Review — attempt 1

Re-walked the seven leaf FRDs in `requirements/features/` (`prd-authoring`, `requirements-loop`, `blueprint-loop`, `work-orders-loop`, `coding-loop`, `on-disk-layout`, `python-orchestrator`). No `<slug>_children/` directories exist anywhere under the features tree, so parent/child correctness is trivially satisfied. Scoping shape across the tree is sound and unchanged from the prior pass review. No critical issues.

Each loop FRD passes the four feature-unit criteria: each delivers standalone operator-visible value (a triggerable subcommand producing a distinct artifact tree, or — for the coding loop — a stack of PRs ready for merge), each carries its own implementation footprint (subcommand + generator skill + reviewer set + state file + per-loop communication folder where applicable), each is independently deployable and testable against its committed inputs, and each adds incremental value beyond its upstream dependencies. `prd-authoring` is correctly scoped as the operator-facing interactive stage with its own skill footprint distinct from the autonomous loops. `on-disk-layout` and `python-orchestrator` are cross-cutting infrastructure FRDs that pin the canonical layout contract and the orchestrator runtime respectively; each has been part of the canonical feature set throughout, each has standalone implementation surface (the layout contract / the Python orchestrator program), and treating them as feature units keeps those contracts in their own documents rather than scattering them across every loop FRD. Consistent with prior reviews.

The work-orders-loop / coding-loop split holds: REQ-WO-001..010 covers blueprint→work-order decomposition (single artifact tree under `work-orders/`, three reviewers, `awaiting_clarification` mechanism, gen↔reviewer communication folder); REQ-CL-001..007 covers ready-queue drain (per-WO subprocess, six-reviewer execution stack, PR-based comms, no communication folder). AC-CL-001.3 and AC-WO-001.4 explicitly cross-reference to anchor the boundary, and the per-WO document shape (REQ-WO-002) is pinned in the work-orders-loop FRD with the coding-loop and on-disk-layout FRDs (AC-LAYOUT-005.4, REQ-CL-004) referencing it as the contract rather than redefining it. No requirement is duplicated across the two FRDs; nothing falls between them.

Cross-cutting overlap between `on-disk-layout` / `python-orchestrator` / loop FRDs (communication-folder lifecycle, meta-file materialisation, verdict aggregation) is correctly partitioned by aspect: layout pins the on-disk shape, orchestrator pins the runtime behavior, each loop FRD pins its loop-specific application. That is consistency, not lumping — these are distinct concerns within the same mechanism, and any one of those FRDs could be reviewed and changed independently without the other two needing to follow. Whole-tree terminology consistency is `req-cross-doc-judge`'s remit, not mine.
